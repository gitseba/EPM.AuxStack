<UserControl x:Class="LogProcessor.Wpf.Views.MetadataView"
				 xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
				 xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
				 xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
				 xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
				 mc:Ignorable="d"
				 xmlns:b="http://schemas.microsoft.com/xaml/behaviors"
				 xmlns:local="clr-namespace:LogProcessor.Wpf.Views"
				 xmlns:converters="clr-namespace:LogProcessor.Wpf.Converters"
				 xmlns:prism="http://prismlibrary.com/"
				 prism:ViewModelLocator.AutoWireViewModel="True">

	<b:Interaction.Triggers>
		<b:EventTrigger EventName="Loaded">
			<b:CallMethodAction TargetObject="{Binding}"
									  MethodName="InitialLoad" />
		</b:EventTrigger>
	</b:Interaction.Triggers>
	
	<Grid>
		<ListView ItemsSource="{Binding FilteredMetadata}"
					 AlternationCount="2">
			<ListView.ItemTemplate>
				<DataTemplate>
					<StackPanel Orientation="Vertical">
						<TextBlock Text="{Binding Key}"
									  FontWeight="Bold"
									  Margin="0 4 0 0" />
						<TextBlock Text="{Binding Value}" />
					</StackPanel>
				</DataTemplate>
			</ListView.ItemTemplate>
		</ListView>
	</Grid>
</UserControl>
